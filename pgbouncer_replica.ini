#pgbouncer.ini

[databases]
* = host = postgres_replica port=5432 user=betterbooks password=betterbooks dbname=betterbooks

[pgbouncer]
listen_addr = 0.0.0.0
auth_type = any
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres

# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts

