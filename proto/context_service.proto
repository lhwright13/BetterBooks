// gRPC definitions for the Context Service

syntax = "proto3";

package betterbooks.context;

service ContextService {
  rpc GetContext (ContextRequest) returns (ContextResponse);
}

message ContextRequest {
  string book_id = 1;
  string query = 2;
}

message ContextResponse {
  string context = 1;
}
