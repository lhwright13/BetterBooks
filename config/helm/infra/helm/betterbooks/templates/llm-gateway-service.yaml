apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: {{ .Values.global.namespace | default "betterbooks" }}
  labels:
    app: llm-gateway
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: llm-gateway