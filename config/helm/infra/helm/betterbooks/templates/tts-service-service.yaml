apiVersion: v1
kind: Service
metadata:
  name: tts-service
  namespace: {{ .Values.global.namespace | default "betterbooks" }}
  labels:
    app: tts-service
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: tts-service