apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboards
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  echowright-overview.json: |
{{- .Files.Get "dashboards/echowright-overview.json" | nindent 4 }}