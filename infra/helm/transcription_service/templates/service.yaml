apiVersion: v1
kind: Service
metadata:
  name: {{ include "transcription_service.fullname" . }}
  labels:
    {{- include "transcription_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "transcription_service.selectorLabels" . | nindent 4 }}